import UserForm from "./components/UserForm";

const UserAdd = () => {
  return (
    <div className="page">
      <h2>Add New User</h2>
      <UserForm />
    </div>
  );
};

export default UserAdd;
